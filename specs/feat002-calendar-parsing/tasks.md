# 任务（FEAT-002-日历解析修复）

- T1 保存原始输出：落盘 `data/calendar/raw/week-<ISO周>.txt`，供溯源。
- T1.5 原始一周分析：直接跑一周的 TXT 原始数据，与日志一起人工分析并总结解析规则（命名/时间段模式/尾部噪声），确定规则后再编码。
- T2 小范围验证：实现 `--sample-day` 仅解析指定日，迭代正则。
- T3 宽松解析：支持 yesterday/today/绝对日期时间段，截断 `@url/notes:`，解析失败写日志。
- T4 过滤策略：白名单过滤后为空则保留全部并记录。
- T5 全量生成：去掉 sample-day，生成 JSON，检查 count 与原始行匹配度。
