# Leadership 对齐记录

## 2025-12-11 宪章编号与 Leadership 记录对齐
- 问题摘要：宪章新增 Leadership 条款后未在 Leadership.md 留痕，且编号与后续原则重复，需要明确顺延编号与记录流程。
- 之前观点（你）：每次反复沟通应在 Leadership.md 记录，避免遗漏；宪章新增项需顺序追加、编号不重复。
- 之前处理（我）：先调整宪章编号，但未同步在 Leadership.md 留痕，造成你再次提醒。
- 最新共识/决定：已在宪章中明确“新增原则按序号顺延追加”，Leadership 为第10条，后续条目顺延；本条作为沟通留痕，后续类似变更需同步记录。
- 待办提醒：当前 `.gitignore`、宪章、specs/spec.md 等改动尚未提交；`specs/file-reflect-and-summary/` 仍为本地忽略目录，保持不入库。

## 2025-12-11 分支/提交前缀约定澄清
- 问题摘要：使用 `chore` 作为提交前缀未在既有规范中约定，需要明确是否接受及使用范围。
- 之前观点（你）：主要使用 `feat(编号)：...` 作为提交格式；`chore` 未明确约定。
- 之前处理（我）：本次提交使用 `chore(F009)` 标记宪章与记录类更新。
- 最新共识/决定：可保留 `chore` 用于非功能/非修复的维护类改动，但需在分支/提交规范中补充说明，避免歧义。

## 2025-12-10 file-reflect-and-summary 命名与入库范围
- 问题摘要：日/周反思目录命名（file-reflect-and-summary）与是否纳入 Git 记录存在反复沟通。
- 之前观点（你）：希望保留日/周反思、模板 mtime 缓存，但不希望产物进入仓库；目录命名需与文档 AI 协作区分。
- 之前处理（我）：曾将反思目录、log 纳入 git 并改名，后被要求撤销；造成多次往返。
- 最新共识：目录改为 `specs/file-reflect-and-summary/` 仅本地使用，已在 `.git/info/exclude` 忽略；git 只保留总览条目说明，不追踪 daily/weekly 产物或模板缓存。
- 待办提醒：日模板（ID 146EF927-021D-4BD8-B70B-1377605704D6）尚未导出到 `artifacts/bear-notes/`；如需同步其他机器，请手动添加相同忽略规则。
