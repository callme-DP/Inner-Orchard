# 计划（FEAT-003-数据管道）

## 步骤
- 建立数据源适配：日历（icalBuddy/JSON）、ActivityWatch（API 聚合）、Mood（本地记录）。
- 统一输出结构：日/周级 JSON，接口与当前 mock 对齐。
- 一键生成脚本：串联数据源 → 规范化 → 注入模板。
- 回退与提示：缺权限/缺数据时使用占位并记录日志。

## 验证
- 本地运行生成 weekly HTML，无报错；类别/时长/事件数量与源数据一致。
- 缺任一数据源时，页面提示且仍可打开。
